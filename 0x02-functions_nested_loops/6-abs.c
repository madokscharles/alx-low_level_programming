#include "main.h"

/**
 * _abs - the aboslute number of an integer
 * @i: variable to determine the absolute value of an integer
 * main - main
 * Return: 0
 */

int _abs(int i)
{
	if (i < 0)
	{
		i *= -1;
	}

	return (i);
}
